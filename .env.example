# Model path — gunakan 'yolov8n.pt' untuk pretrained COCO,
# atau path ke model custom setelah training dengan data karung
SACK_MODEL_PATH=yolov8n.pt

# Minimum confidence score untuk menganggap deteksi valid (0.0 - 1.0)
CONFIDENCE_THRESHOLD=0.40

# Port web server
PORT=8000

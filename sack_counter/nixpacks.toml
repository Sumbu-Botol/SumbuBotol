# Nixpacks build config untuk Railway
# OpenCV headless membutuhkan beberapa library sistem

[phases.setup]
nixPkgs = ["ffmpeg"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt",
]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port $PORT"
